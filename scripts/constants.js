// How many distinct cat images are there?
const NUM_CATS = 13;

// The delay before the game starts
const GAME_DELAY = 1000;

// The width and height of a building
const BUILDING_SIZE = 50;

// The width of a road
const ROAD_WIDTH = 20;

export { NUM_CATS, GAME_DELAY, BUILDING_SIZE, ROAD_WIDTH };
