// How many distinct cat images are there?
const NUM_CATS = 13;

// The delay before the game starts
const GAME_DELAY = 0;

// The width and height of a building
const BUILDING_SIZE = 50;

// The width of a road
const ROAD_WIDTH = 20;

// The minimum loading time
const MIN_LOAD = 0;

// The time it takes to fade
const FADE_TIME = 0;

export { NUM_CATS, GAME_DELAY, BUILDING_SIZE, ROAD_WIDTH, MIN_LOAD, FADE_TIME };
